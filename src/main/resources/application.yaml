spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hostel
    username: postgres
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: Hostel
